# Usage

## Installation

To install tamlep-package:

```console
(.venv) $ git clone https://github.com/RakeshSTA/mle-training.git
(.venv) $ cd mle-training
(.venv) $ poetry env use <path_reqd_version_of_python (3.12+)>
(.venv) $ poetry install
```
